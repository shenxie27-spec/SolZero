# 将 api.solzero.top 替换为实际域名，并确保 DNS 已解析到本机
# 强制使用正式版 Let's Encrypt CA，避免误用 staging 测试证书
{
    acme_ca https://acme-v02.api.letsencrypt.org/directory
}

api.solzero.top {
    encode gzip
    reverse_proxy server:8787
}
